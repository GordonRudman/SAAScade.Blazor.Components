@typeparam TItem
@foreach (var item in Items ?? Array.Empty<TItem>())
{
	@if (ShowItemsWhen)
	{
		@ChildContent(item)
	}
}
@code
{
	[Parameter] public RenderFragment<TItem> ChildContent { get; set; }
	[Parameter] public IEnumerable<TItem> Items { get; set; }
	[Parameter] public bool ShowItemsWhen { get; set; } = true;
}